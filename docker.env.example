# Copier vers docker.env (non versionne) : cp docker.env.example docker.env
# Utiliser : docker compose --env-file docker.env up --build

MYSQL_ROOT_PASSWORD=imw-dev-root
# MYSQL_PUBLISH_PORT=3307

JWT_SECRET_KEY=change-me-dev-team
GEMINI_API_KEY=

# FLASK_USE_RELOADER=false
# CHOKIDAR_USEPOLLING=false
